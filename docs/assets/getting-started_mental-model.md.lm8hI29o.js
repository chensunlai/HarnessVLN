import { _ as _export_sfc, o as openBlock, c as createElementBlock, a2 as createStaticVNode } from "./chunks/framework.DqlImjjO.js";
const __pageData = JSON.parse('{"title":"先建立正确心智模型","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/mental-model.md","filePath":"getting-started/mental-model.md","lastUpdated":null}');
const _sfc_main = { name: "getting-started/mental-model.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="先建立正确心智模型" tabindex="-1">先建立正确心智模型 <a class="header-anchor" href="#先建立正确心智模型" aria-label="Permalink to &quot;先建立正确心智模型&quot;">​</a></h1><p>HarnessVLN 不是把每个模拟器包装成 <code>reset / observe / act</code> 后再由 Runner 逐步驱动的评测脚本。 它把<strong>完整导航任务的控制权交给 Agent</strong>，Runner 只负责准备和调度任务。</p><h2 id="从两种循环的差别开始" tabindex="-1">从两种循环的差别开始 <a class="header-anchor" href="#从两种循环的差别开始" aria-label="Permalink to &quot;从两种循环的差别开始&quot;">​</a></h2><p>传统 Runner 主导循环通常是：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Runner: reset -&gt; observe -&gt; Agent.predict -&gt; env.step -&gt; observe -&gt; ... -&gt; score</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这种结构默认“每次观测只产生一次动作”，会把 VLN 模型自己的视频缓存、异步大小脑、连续轨迹 或多频控制拆散。HarnessVLN 的调用关系是：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Runner -&gt; NavigationHarness.run_task(task, stack)</span></span>\n<span class="line"><span>                         |</span></span>\n<span class="line"><span>                         +-&gt; Agent.run(context)       # 整个 Task 只调用一次</span></span>\n<span class="line"><span>                               |-- nav.observe</span></span>\n<span class="line"><span>                               |-- nav.move.discrete</span></span>\n<span class="line"><span>                               |-- vln.navigate.local (blocking)</span></span>\n<span class="line"><span>                               |       `-- VLN 内部反向调用 observe / move</span></span>\n<span class="line"><span>                               |-- spatial.search / remember</span></span>\n<span class="line"><span>                               |-- nav.goal.finish</span></span>\n<span class="line"><span>                               `-- nav.stop</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>Agent 可以写成固定 workflow，也可以是完全自由的函数调用循环。Harness 只规定生命周期和工具 边界，不规定 <code>Agent.run</code> 内部要执行多少轮、每轮调用什么。</p><h2 id="四个容易混淆的角色" tabindex="-1">四个容易混淆的角色 <a class="header-anchor" href="#四个容易混淆的角色" aria-label="Permalink to &quot;四个容易混淆的角色&quot;">​</a></h2><div class="concept-grid"><div class="concept-card"><strong>Runner</strong><p>选择并调度一个或多个完整 Bench session，应用并发/超时/输出策略。它不定义 case、不读取观测，也不产生动作。</p></div><div class="concept-card"><strong>NavigationHarness</strong><p>装配一个 Task 的组件，建立 ToolBus，处理终止竞争，并保证资源按顺序回收。</p></div><div class="concept-card"><strong>Agent Core</strong><p>唯一决策主体。它决定直接行动、委托 VLN、查询记忆、切换子目标或终止任务。</p></div><div class="concept-card"><strong>VLN plugin</strong><p>一套完整导航模型。模型内部的状态、推理频率、线程和轨迹策略留在插件内部。</p></div></div><p>Environment 与 Bench 也必须分开理解：Environment 管原生仿真/真机执行，Bench 管 case 加载、 私有真值和评分。配置上由 Bench 引用匹配的 Env，但代码契约仍独立；Env 只收到不含评分真值的 <code>EnvironmentEpisode</code>。</p><h2 id="env、bench、runner-与-run" tabindex="-1">Env、Bench、Runner 与 Run <a class="header-anchor" href="#env、bench、runner-与-run" aria-label="Permalink to &quot;Env、Bench、Runner 与 Run&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>RunnerConfig -&gt; BenchConfig[] -&gt; EnvConfig</span></span>\n<span class="line"><span>AgentConfig  -&gt; Agent + VLN? + Memory?</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><ul><li><strong>Env</strong> 回答“这个 Task 在哪个系统中怎样观察和行动”；</li><li><strong>Bench</strong> 回答“产生哪些 Task、允许怎样 reset、最后怎样评分”；</li><li><strong>Runner</strong> 回答“这次批量执行哪些 Bench、允许多少完整 Task 并发、输出到哪里”；</li><li><strong>Run</strong> 只是 Runner 的一次执行实例及其 Manifest，不提供观测，也不实现 Agent loop。</li></ul><p>结果结构同样遵守这层含义：一次 <code>RunSummary</code> 包含多个 <code>BenchmarkSummary</code>，每个 <code>BenchmarkSummary</code> 再包含有序的 <code>CaseRecord</code>。单个 Bench 的结果不会再被命名成 Run。</p><p><code>env</code> CLI 模式没有 Bench/Runner：用户输入临时扮演任务源，只选择 Env 与 Agent。进入 Harness 后的 Task 生命周期与批量模式相同。</p><h2 id="agent-与-vln-不是同一个抽象" tabindex="-1">Agent 与 VLN 不是同一个抽象 <a class="header-anchor" href="#agent-与-vln-不是同一个抽象" aria-label="Permalink to &quot;Agent 与 VLN 不是同一个抽象&quot;">​</a></h2><ul><li><strong>Agent</strong> 回答“这项任务现在应该组织哪些能力”。例如先查空间记忆、拆成两段、调用 VLN， 失败后重新观察，最后停止。</li><li><strong>VLN</strong> 回答“给定一条导航指令，模型内部如何完成导航”。例如 StreamVLN 保留帧历史， DualVLN 保留 System 1 / System 2 的异步节奏。</li><li>Agent 可以完全不接 VLN，只用 <code>nav.*</code> 工具；NormalAgent 只给 VLN 当前可见的局部目标，专用 Passthrough Agent 才把任务指令原样交给 VLN。</li><li>同一 VLN 无需知道上层使用透传 Agent 还是工作流 Agent。</li></ul><p>因此二者在 Task 运行期可以同时存活、异步推进，但它们不是“两个平级 Agent”。Agent 发起阻塞 VLN 调用；navigator 在内部管理 Job，VLN worker 获得受限的反向工具客户端，自主观察和行动。</p><h2 id="三层稳定边界" tabindex="-1">三层稳定边界 <a class="header-anchor" href="#三层稳定边界" aria-label="Permalink to &quot;三层稳定边界&quot;">​</a></h2><ol><li><strong>公开任务边界</strong>：Agent 只拿到 <code>NavTask</code>，未来 Goal 和评分真值不进入上下文。</li><li><strong>工具边界</strong>：组件只能调用 <code>required_tools</code> 声明过的工具，参数逐次经过 JSON Schema 校验。</li><li><strong>原生环境边界</strong>：Habitat、AI2-THOR、Isaac 对象只存在于 Environment 内部。</li></ol><p>这三层使新模型、新模拟器和新 Agent 能独立变化。兼容性不是靠巨大的统一观测字典，而是靠 <code>NavigationProfile</code> 在启动前验证当前 VLN 所需的通道、动作和相机参数。</p><h2 id="一句话判断模块该放哪里" tabindex="-1">一句话判断模块该放哪里 <a class="header-anchor" href="#一句话判断模块该放哪里" aria-label="Permalink to &quot;一句话判断模块该放哪里&quot;">​</a></h2><table tabindex="0"><thead><tr><th>问题</th><th>归属</th></tr></thead><tbody><tr><td>怎样遍历 split、读取 episode、算 SPL？</td><td>Bench</td></tr><tr><td>怎样把 <code>forward</code> 映射为 Habitat action id？</td><td>Environment</td></tr><tr><td>怎样把一条指令拆成多个导航段？</td><td>Agent</td></tr><tr><td>怎样维护图像历史并预测动作？</td><td>VLN</td></tr><tr><td>怎样检索跨 Task 的地标？</td><td>Memory</td></tr><tr><td>怎样校验调用、封住停止后的运动？</td><td>ToolBus / Harness</td></tr><tr><td>怎样限制同时运行几个完整 case？</td><td>Runner</td></tr></tbody></table><p>下一步：<a href="./quick-start.html">十分钟运行</a>。</p>', 25)
  ])]);
}
const mentalModel = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  mentalModel as default
};
