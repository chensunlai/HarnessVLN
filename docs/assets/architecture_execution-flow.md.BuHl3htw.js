import { _ as _export_sfc, o as openBlock, c as createElementBlock, a2 as createStaticVNode } from "./chunks/framework.DqlImjjO.js";
const _imports_0 = "/HarnessVLN/task-lifecycle.png";
const __pageData = JSON.parse('{"title":"一次任务如何运行","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/execution-flow.md","filePath":"architecture/execution-flow.md","lastUpdated":null}');
const _sfc_main = { name: "architecture/execution-flow.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="一次任务如何运行" tabindex="-1">一次任务如何运行 <a class="header-anchor" href="#一次任务如何运行" aria-label="Permalink to &quot;一次任务如何运行&quot;">​</a></h1><p><code>NavigationHarness.run_task(task, stack)</code> 是单 Task 的生命周期边界。它只调用一次 <code>Agent.run(context)</code>，同时监听环境终止和 Agent 主动停止。</p><p><img src="' + _imports_0 + '" alt="单 Task 生命周期" class="architecture-asset"></p><h2 id="_1-校验-stack-并建立-toolbus" tabindex="-1">1. 校验 stack 并建立 ToolBus <a class="header-anchor" href="#_1-校验-stack-并建立-toolbus" aria-label="Permalink to &quot;1. 校验 stack 并建立 ToolBus&quot;">​</a></h2><p><code>NavigationStack</code> 组装时先校验 Agent、Environment 以及可选 VLN/Memory 的必需方法和不可变 <code>required_tools</code> 声明。若存在 VLN，Harness 在任何组件启动前比较它的 requirements 与 Environment profile；结构或组合错误不会先拉起仿真器。</p><p>随后 Harness 创建新的 <code>ToolBus</code>、取消事件和 terminal signal，并注册 <code>nav.stop</code>。<code>nav.stop</code> 只负责竞争终止状态，不直接调用 Environment 的原生 Stop 动作；当前 Goal 的提交由 <code>nav.goal.finish</code> 完成。</p><p>Agent 获得的 <code>NavContext</code> 包含：</p><div class="language-python vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">NavContext(</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    task</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">task,</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    execution_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">uuid,</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    tools</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">agent_tool_client,</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    cancelled</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cancel_event,</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    output</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">agent_output_scope,</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p><code>context.nav</code>、<code>context.vln</code>、<code>context.spatial</code> 是对 ToolClient 的轻量类型化便捷封装。</p><h2 id="_2-按资源依赖启动组件" tabindex="-1">2. 按资源依赖启动组件 <a class="header-anchor" href="#_2-按资源依赖启动组件" aria-label="Permalink to &quot;2. 按资源依赖启动组件&quot;">​</a></h2><p>启动顺序固定为：</p><ol><li><code>Environment.start(task, env_output)</code>：先注册观测、动作、goal 与可选原生规划工具；</li><li>若配置 Memory，执行 <code>Memory.start(task, memory_client, memory_output)</code> 并注册空间工具；</li><li>若配置 VLN，执行 <code>VLN.start(task, vln_client, vln_output)</code> 并注册阻塞 VLN 工具；</li><li>校验 Agent 的 required tools 已全部存在；</li><li>创建 <code>Agent.run</code>、<code>Environment.wait_terminal</code> 和 terminal wait 三个协程。</li></ol><p>工具先注册再验证，缺失能力会在 Agent 开始决策前快速失败。 每个组件的工具批次会先整体校验再写入 ToolBus，注册失败不会暴露半批工具。</p><p>Harness 在调用每个组件的 <code>start</code> 前先登记其生命周期。因此 Environment、Memory 或 VLN 在 <code>start</code> 内分配了部分资源后抛异常，<code>finally</code> 仍会调用该组件的 <code>stop</code>；尚未进入 <code>start</code> 的组件 不会收到伪造的 stop。</p><h2 id="_3-agent-主导运行" tabindex="-1">3. Agent 主导运行 <a class="header-anchor" href="#_3-agent-主导运行" aria-label="Permalink to &quot;3. Agent 主导运行&quot;">​</a></h2><p>以透传 Agent 为例，真实控制流是：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Agent                  VLN navigator / worker          Environment</span></span>\n<span class="line"><span>  | observe ------------------------------------------------&gt;|</span></span>\n<span class="line"><span>  |&lt;---------------------------- current RGB / pose --------|</span></span>\n<span class="line"><span>  | navigate.local(visible target)                           |</span></span>\n<span class="line"><span>  |----------------------------&gt;|-- observe ---------------&gt;|</span></span>\n<span class="line"><span>  |                             |&lt;---------------------------|</span></span>\n<span class="line"><span>  |                             |-- move(forward) ---------&gt;|</span></span>\n<span class="line"><span>  |                             |&lt;--------------------------|</span></span>\n<span class="line"><span>  |             (blocked)       |        ...               |</span></span>\n<span class="line"><span>  |&lt;---------------- terminal --|                          |</span></span>\n<span class="line"><span>  | finish_goal ------------------------------------------&gt;|</span></span>\n<span class="line"><span>  |&lt;---------------------------------- done / next goal ----|</span></span>\n<span class="line"><span>  | stop(task)                  |                          |</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p>Agent 只发起一次阻塞调用，不接触 Job id 或状态轮询。VLN navigator 内部管理 RPC Job，worker 可以按自己的频率执行多次 observe/move，也可运行内部异步线程。NormalAgent 返回后重新观察并选择 下一个局部可视目标；Passthrough Agent 则用 <code>navigate.task</code> 一次传递完整任务。</p><p>固定 workflow Agent 同样在一个 <code>run</code> 中执行多个步骤。自由 Agent loop 则可以在每轮选择任意 已授权工具；Harness 不需要增加另一个运行模式。</p><h2 id="_4-三方终止竞争" tabindex="-1">4. 三方终止竞争 <a class="header-anchor" href="#_4-三方终止竞争" aria-label="Permalink to &quot;4. 三方终止竞争&quot;">​</a></h2><p>Harness 等待以下事件中第一个完成：</p><table tabindex="0"><thead><tr><th>信号</th><th>典型来源</th><th>结果 actor</th></tr></thead><tbody><tr><td><code>nav.stop</code></td><td>Agent 判断任务完成或失败</td><td>调用者 actor</td></tr><tr><td>Environment terminal</td><td>原生 episode 结束、动作预算、服务失败</td><td><code>environment</code></td></tr><tr><td>Agent 正常返回但没 stop</td><td>Agent 实现错误</td><td><code>harness</code> failed</td></tr><tr><td>Agent 抛异常</td><td>模型/工作流错误</td><td><code>agent</code> failed</td></tr><tr><td><code>timeout_s</code></td><td>Task 超时</td><td><code>harness</code> failed</td></tr></tbody></table><p>Terminal signal 由锁保护，第一位 claim 者获胜。后到的 stop 仍拿到同一个 terminal，不覆盖原因。</p><h2 id="_5-先封写-再回收" tabindex="-1">5. 先封写，再回收 <a class="header-anchor" href="#_5-先封写-再回收" aria-label="Permalink to &quot;5. 先封写，再回收&quot;">​</a></h2><p>进入 <code>finally</code> 后，Harness 先设置 cancelled，并关闭 ToolBus 的新写调用入口。随后：</p><ol><li>停止 Environment，使原生运动不能继续；</li><li>停止 VLN，取消/收敛 Job 和 worker；</li><li>取消仍存活的 Agent 与环境 wait task；</li><li>等待已进入的写工具调用排空；</li><li>停止 Memory，原子持久化写回；</li><li>若 Environment 进入过 start，读取 <code>Environment.result()</code>；</li><li>把 ToolEvent 写入 Episode 审计，并把 cleanup errors 与主 terminal 分开保存。</li></ol><p>这个顺序用于处理最危险的竞态：Task 超时或 Agent 停止时，VLN 可能仍有一个反向 move 在途。 Environment 先停止且 ToolBus 拒绝新写，使旧 worker 无法在下一 Task 中产生运动。</p><p>Task 终止后的活动 VLN Job 由 <code>VLNNavigator.stop()</code> 直接回收；阻塞工具被独立取消时，navigator 也会收敛对应内部 Job，并保留外层 <code>CancelledError</code>。Agent 不需要再发取消工具调用。</p><h2 id="_6-返回结果" tabindex="-1">6. 返回结果 <a class="header-anchor" href="#_6-返回结果" aria-label="Permalink to &quot;6. 返回结果&quot;">​</a></h2><p><code>NavigationResult</code> 至少包含：</p><ul><li><code>execution_id</code></li><li>最终 <code>terminal(status, reason, actor)</code></li><li>Environment 的结构化结果</li><li><code>cleanup_errors</code></li><li>带调用序号与 outcome 的 <code>ToolEvent</code> 审计</li></ul><p>Bench 在 Harness 返回后才评分。即使 task 失败，能够取得的清理与审计信息也保留，便于区分模型 失败、环境失败和基础设施失败。</p>', 32)
  ])]);
}
const executionFlow = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  executionFlow as default
};
