agent:
    factory: agents.normal_agent:NormalAgent
    params:
      model: gpt-5-mini
      max_iterations: 80
      reasoning_summary: false
      commentary: false
      tools:
        - nav.observe
        - nav.move.discrete
        - nav.goal.finish
        - vln.navigate.local
        - spatial.search
        - spatial.remember
    vln: ../vln/dummy.yaml
    memory: ../memory/dummy_landmark.yaml
